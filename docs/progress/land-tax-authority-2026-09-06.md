# 土地状态使用统一税务账本（2026-09-06）

## 本次实现

- 土地状态判断不再只依赖 `LandClaim.taxOwed` 的兼容镜像。
- 转让、出售和接受转让请求时，直接查询统一税务账本的土地税欠款。
- 税务账本与旧存档镜像暂时不一致时，仍以统一账本为准，避免欠税土地被短暂转移或继续操作。

## 设计原则

土地对象中的税额字段保留用于旧存档兼容和界面显示；税务系统是欠税金额和支付状态的唯一权威来源。

## 后续方向

- 将土地拍卖、租约和权限冻结全部收敛到统一土地状态机。
- 增加税务账本与土地镜像的管理员对账和修复命令。
